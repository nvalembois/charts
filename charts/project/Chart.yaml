# Chart.yaml
apiVersion: v2
name: project
description: project Helm Chart that deploys a pg-cluster
type: application
version: 1.0.2
appVersion: 1.0.0

dependencies:
  - name: k8s-store
    repository: https://nvalembois.github.io/charts
    version: 1.0.0
    condition: k8s-store.create

annotations:
  artifacthub.io/changes: |
    - description: Ajout de la création d'un SecretStore k8s-store
      kind: added

