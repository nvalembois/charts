{
    "title": "Chart Values",
    "type": "object",
    "namespace": {
        "type": "string",
        "description": "Objects namespace",
        "default": "{{ .Release.Namespace }}"
    },
    "name": {
        "type": "string",
        "description": "Objects name",
        "default": "{{ .Release.Name }}"
    }
}